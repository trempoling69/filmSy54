<form method="post" action="{{ path('app_type_artefact_delete', {'id': type_artefact.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_artefact.id) }}">
    <button class="btn">Delete</button>
</form>
